<Window x:Class="GestorStock.API.CreatePedidoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GestorStock.API"
        xmlns:converters="clr-namespace:GestorStock.API.Converters"
        mc:Ignorable="d"
        x:Name="CreatePedidoWindowInstance"
        Title="Crear/Editar Pedido" Height="650" Width="800">

    <Window.Resources>
        <converters:RepuestosTotalConverter x:Key="RepuestosTotalConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,10">
            <TextBlock Text="Detalles del Pedido" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Fecha de Llegada:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <DatePicker x:Name="FechaLlegadaDatePicker" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Descripción:" VerticalAlignment="Center" Margin="0,5,10,0"/>
                <TextBox x:Name="DescripcionTextBox" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="0,5,0,0"/>

                <CheckBox x:Name="IncidenciaCheckBox" Content="Tiene Incidencia" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,10,0,0"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Fecha Incidencia:" VerticalAlignment="Center" Margin="0,5,10,0"/>
                <DatePicker x:Name="IncidenciaDatePicker" Grid.Row="3" Grid.Column="1" VerticalAlignment="Center" Margin="0,5,0,0"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Descripción de Incidencia:" VerticalAlignment="Center" Margin="0,5,10,0"/>
                <TextBox x:Name="DescripcionIncidenciaTextBox" Grid.Row="4" Grid.Column="1" VerticalAlignment="Center" Margin="0,5,0,0"/>
            </Grid>
        </StackPanel>

        <TextBlock Text="Ítems del Pedido" FontWeight="Bold" FontSize="16" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="0,10,0,10"/>

        <DataGrid x:Name="ItemsDataGrid" Grid.Row="2" Grid.ColumnSpan="2" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ubicación/Producto" Binding="{Binding UbicacionProducto.Nombre}" Width="*"/>
                <DataGridTextColumn Header="Familia" Binding="{Binding UbicacionProducto.Familia.Nombre}" Width="*"/>
                <DataGridTextColumn Header="Tipo Soporte" Binding="{Binding TipoSoporte.Nombre}" Width="*"/>
                <DataGridTextColumn Header="Total (€)" Binding="{Binding Path=Repuestos, Converter={StaticResource RepuestosTotalConverter}, StringFormat=C}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="AddItemButton" Content="Añadir Ítem" Grid.Column="0" Margin="5"/>
            <Button x:Name="EditItemButton" Content="Editar Ítem" Grid.Column="1" Margin="5"/>
            <Button x:Name="DeleteItemButton" Content="Eliminar Ítem" Grid.Column="2" Margin="5"/>
        </Grid>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="GuardarPedidoButton" Content="Guardar Pedido" Width="120" Margin="5"/>
            <Button x:Name="CancelarButton" Content="Cancelar" Width="120" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>