<Window x:Class="GestorStock.API.EditRepuestoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GestorStock.API"
        mc:Ignorable="d"
        Title="Editar Repuesto" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Tipo de Repuesto:" FontWeight="Bold" Margin="0,5,10,5"/>
        <ComboBox x:Name="TipoRepuestoComboBox" Grid.Row="0" Grid.Column="1" 
                  Margin="0,5,0,5" DisplayMemberPath="Nombre"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Nombre del Repuesto:" FontWeight="Bold" Margin="0,5,10,5"/>
        <TextBox x:Name="RepuestoTextBox" Grid.Row="1" Grid.Column="1" 
                 Margin="0,5,0,5" Text="{Binding Nombre}"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Cantidad:" FontWeight="Bold" Margin="0,5,10,5"/>
        <TextBox x:Name="CantidadTextBox" Grid.Row="2" Grid.Column="1" 
                 Margin="0,5,0,5" Text="{Binding Cantidad}"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="AceptarButton" Content="Aceptar" Width="80" Margin="0,0,10,0" Click="AceptarButton_Click"/>
            <Button x:Name="CancelarButton" Content="Cancelar" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>