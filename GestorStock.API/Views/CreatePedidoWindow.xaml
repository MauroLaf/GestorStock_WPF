<Window x:Class="GestorStock.API.Views.CreatePedidoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Crear/Editar Pedido" Width="1000" Height="680"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- CABECERA -->
        <Grid Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                <TextBlock Text="Familia:" FontWeight="Bold"/>
                <ComboBox x:Name="cmbFamilia" DisplayMemberPath="Nombre" SelectionChanged="cmbFamilia_SelectionChanged"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="0,0,8,0">
                <TextBlock Text="Ubicación/Producto:" FontWeight="Bold"/>
                <ComboBox x:Name="cmbUbicacion" DisplayMemberPath="Nombre"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Margin="0,0,8,0">
                <TextBlock Text="Proveedor:" FontWeight="Bold"/>
                <ComboBox x:Name="cmbProveedor" DisplayMemberPath="Nombre"/>
            </StackPanel>

            <StackPanel Grid.Column="3">
                <TextBlock Text="Tipo de Soporte:" FontWeight="Bold"/>
                <ComboBox x:Name="cmbTipoSoporte" DisplayMemberPath="Nombre"/>
            </StackPanel>
        </Grid>

        <!-- INFO PEDIDO -->
        <Border Grid.Row="1" Padding="10" Background="#FFF9F3" BorderBrush="#DDD" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,12,0">
                        <TextBlock Text="Descripción del pedido:" FontWeight="Bold"/>
                        <TextBox x:Name="txtDescripcionPedido"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,12,0" VerticalAlignment="Bottom">
                        <TextBlock Text="Llegada:" FontWeight="Bold"/>
                        <DatePicker x:Name="dpFechaLlegada"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="12,0" VerticalAlignment="Bottom">
                        <TextBlock Text="Incidencia:" FontWeight="Bold"/>
                        <CheckBox x:Name="chkIncidencia"/>
                    </StackPanel>

                    <StackPanel Grid.Column="3" Margin="12,0,0,0" VerticalAlignment="Bottom">
                        <TextBlock Text="Fecha Incidencia:" FontWeight="Bold"/>
                        <DatePicker x:Name="dpFechaIncidencia"/>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="1" Margin="0,0,0,8">
                    <TextBlock Text="Descripción de la incidencia:" FontWeight="Bold"/>
                    <TextBox x:Name="txtDescripcionIncidencia" />
                </StackPanel>

                <!-- DETALLE -->
                <DataGrid x:Name="dgRepuestos" Grid.Row="2"
                          AutoGenerateColumns="False" CanUserAddRows="False"
                          IsReadOnly="True" HeadersVisibility="Column"
                          Margin="0,6,0,6">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="200"/>
                        <DataGridTextColumn Header="Descr"   Binding="{Binding Descripcion}" Width="200"/>
                        <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}" Width="80"/>
                        <DataGridTextColumn Header="Precio"   Binding="{Binding Precio, StringFormat={}{0:0.00}}" Width="100"/>
                        <DataGridTextColumn Header="Tipo"     Binding="{Binding TipoRepuesto}" Width="120"/>
                        <DataGridTextColumn Header="Ubicación" Binding="{Binding UbicacionProducto.Nombre}" Width="200"/>
                        <DataGridTextColumn Header="Soporte"   Binding="{Binding TipoSoporte.Nombre}" Width="160"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Botonera detalle (sin Spacing) -->
                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                    <StackPanel.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Margin" Value="8,0,0,0"/>
                        </Style>
                    </StackPanel.Resources>
                    <Button Content="Agregar línea" Click="BtnAgregarLinea_Click"  Style="{StaticResource ModernButtonStyle}" Margin="0,0,0,0"/>
                    <Button Content="Editar línea"  Click="BtnEditarLinea_Click"   Style="{StaticResource ModernButtonStyle}"/>
                    <Button Content="Quitar línea"  Click="BtnQuitarLinea_Click"   Style="{StaticResource ModernButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- GUARDAR/CANCELAR -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="8,0,0,0"/>
                </Style>
            </StackPanel.Resources>
            <Button Content="Guardar" Click="BtnGuardar_Click" Style="{StaticResource ModernButtonStyle}" Margin="0,0,0,0"/>
            <Button Content="Cancelar" Click="BtnCancelar_Click" Style="{StaticResource ModernButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
